# requirements.txt برای Steam Price Calculator

# کتابخانه اصلی برای رابط کاربری گرافیکی
PyQt5==5.15.9

# کتابخانه برای درخواست‌های HTTP و دریافت قیمت از سایت‌ها
requests==2.31.0

# کتابخانه برای تجزیه و تحلیل HTML صفحات وب
beautifulsoup4==4.12.2

# کتابخانه برای کار با عبارات منظم (به صورت پیش‌فرض با پایتون نصب است - فقط برای رفع وابستگی)
# re

# کتابخانه برای کار با JSON (به صورت پیش‌فرض با پایتون نصب است)
# json

# کتابخانه برای کار با سیستم عامل (به صورت پیش‌فرض با پایتون نصب است)
# os

# کتابخانه برای باز کردن لینک در مرورگر (به صورت پیش‌فرض با پایتون نصب است)
# webbrowser

# کتابخانه برای تاریخ و زمان (به صورت پیش‌فرض با پایتون نصب است)
# datetime

# کتابخانه برای تردینگ و اجرای همزمان (به صورت پیش‌فرض با پایتون نصب است)
# threading